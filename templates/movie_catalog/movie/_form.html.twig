<div class="align-items-center series mb-4">
    {{ form_start(ratingForm) }}
        <div class="form-group col-sm-3 paddingLeft">                                 
            {{ form_row(ratingForm.rating) }}
        </div>    
        <div class="form-group">                                 
            {{ form_rest(ratingForm) }}
        </div>                 
        
        <button type="submit" class="btn btn-hover">Noter</button>
    {{ form_end(ratingForm) }}
</div>